{
  "name": "shinsina.github.io",
  "version": "0.0.1",
  "scripts": {
    "build-css": "tailwindcss build -i app.css -o public/style.css --minify",
    "deploy": "npm run build-css && npx gh-pages -d public -t true"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.7",
    "postcss": "^8.4.13",
    "tailwindcss": "^3.0.24"
  },
  "dependencies": {}
}
